# dependencies
node_modules/

# signing key + cloud-session recycle bin - never commit these
_keys/
_to_delete/

# expo
.expo/
web-build/
# app/dist (the exported web build) is committed on purpose so hosts can
# serve it without running the Expo toolchain.

# server runtime data
server/data/

# logs and OS noise
*.log
.DS_Store
_to_delete/
server/tests/.tmp-data/

# env
.env
.env.*
app/.venv/
